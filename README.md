
  # Filter Products based on Brand (Community)

  This is a code bundle for Filter Products based on Brand (Community). The original project is available at https://www.figma.com/design/9BhBlFxQIKno5M06colB3U/Filter-Products-based-on-Brand--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  